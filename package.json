{
  "name": "three-webgpu-cap-shell",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reusable Three.js + WebGPU + Capacitor iOS shell with NativeHaptics. Docs: AGENTS.md, docs/ENGINEERING.md",
  "homepage": "./AGENTS.md",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "npm run build && npx cap sync ios",
    "cap:open": "npx cap open ios",
    "ios": "npm run cap:sync && npm run cap:open",
    "ios:bootstrap": "node scripts/bootstrap-ios.mjs"
  },
  "dependencies": {
    "@capacitor/core": "^8.1.0",
    "@capacitor/ios": "^8.1.0",
    "three": "^0.173.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0",
    "vite": "^5.4.6"
  }
}
